import React from 'react';
import { Button } from 'react-bootstrap';

const App = () => {
  return (
    <div>
      <Button variant="phoenix-primary">Click</Button>
    </div>
  );
};

export default App;
